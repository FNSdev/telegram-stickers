version: '3.7'
services:
  database:
    image: postgres:11.4-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: telegram-stickers
      POSTGRES_USER: telegram-stickers
      POSTGRES_PASSWORD: admin